<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myblogssm.mapper.ArticleMapper">
    <resultMap id="baseMap" type="com.example.myblogssm.entity.Article">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="createtime" property="createTime"/>
        <result column="updatetime" property="updateTime"/>
        <result column="uid" property="uid"/>
        <result column="rcount" property="rcount"/>
        <result column="state" property="state"/>
    </resultMap>
    <insert id="addArticle">
        insert into articleinfo(title, content, uid) values (#{title}, #{content}, #{uid})
    </insert>

    <update id="updateRCount">
        update articleinfo set rcount = rcount+1 where id = #{id}
    </update>

    <update id="updateArticle">
        update articleinfo set title = #{title}, content = #{content}, updatetime = now() where id = #{id} and uid = #{uid}
    </update>

    <select id="queryArticleTotalByUid" resultType="java.lang.Integer">
        select count(*) from articleinfo where uid = #{uid}
    </select>

    <select id="qryUserArtListByUid" resultMap="baseMap">
        select *
        from articleinfo
        where uid = #{uid} order by updatetime desc
    </select>

    <select id="queryArticleById" resultMap="baseMap">
        select *
        from articleinfo
        where id = #{id};
    </select>

    <select id="queryArtListByPage" resultMap="baseMap">
        select * from articleinfo limit #{pageSize} offset #{startIndex}
    </select>

    <select id="queryArtListByTitle" resultMap="baseMap">
        select * from articleinfo where title like CONCAT('%', #{searchInfo}, '%') limit #{pageSize} offset #{startIndex}
    </select>

    <select id="queryArticleCount" resultType="java.lang.Integer">
        select count(*) from articleinfo
        <where>
            <if test="searchInfo != null">
                and title = #{searchInfo}
            </if>
        </where>
    </select>

    <delete id="delArticleById">
        delete from articleinfo where id = #{id} and uid = #{uid}
    </delete>

</mapper>